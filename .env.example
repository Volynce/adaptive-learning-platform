# Пример окружения для adaptive-learning-platform
# Важно:
# - .env НЕ коммитится (секреты и локальные параметры).
# - .env.example коммитится как шаблон.
# - Подключение к БД задаём ТОЛЬКО через DATABASE_URL.

# --- Приложение ---
APP_ENV=local
APP_DEBUG=true

# --- База данных (локально через docker-compose будет на localhost:5432) ---
# Формат: postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+psycopg://adaptive_learning:adaptive_learning@localhost:5432/adaptive_learning

# --- JWT (dev/MVP: access-only) ---
JWT_ALG=HS256
JWT_SECRET=CHANGE_ME_SUPER_SECRET
JWT_ACCESS_TTL_MINUTES=60

# --- Хеширование паролей ---
PWD_HASH_SCHEME=bcrypt
PWD_BCRYPT_ROUNDS=12
